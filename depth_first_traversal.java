import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class depth_first_traversal {
	getdepth_first_traversal(T[] entries) tra = mock(depth_first_traversal(T[] entries).class);

	@Test
	void testdepth_first_traversalmethod() {
		tra = new depth_first_traversal(testing);
		doNothing().when(testing).depth_first_traversal()
		tra.depth_first_traversal(testing);
		verify(getdepth_first_traversal,times(9).testing(tra))
	}

}
