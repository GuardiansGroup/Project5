import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class breadth_first_traversal {
	getBreadthFirstTraversal(T[] entries) tra = mock(getBreadthFirstTraversal(T[] entries).class);

	@Test
	void testgetBreadthFirstTraversalmethod() {
		tra = new getBreadthFirstTraversal(testing);
		doNothing().when(testing).getBreadthFirstTraversal()
		optimal.getBreadthFirstTraversal(testing);
		verify(getBreadthFirstTraversal,times(9).testing(tra))
	}

}
